{
  "name": "StatCard",
  "category": "data",
  "description": "KPI stat card with label, value, delta badge, and optional accent.",
  "propsSchema": {
    "label": { "type": "string", "required": true, "description": "Stat label" },
    "value": { "type": "string", "required": true, "description": "Stat value" },
    "delta": { "type": "string", "required": false, "description": "Change indicator text" }
  },
  "template": "import React from 'react';\n\ninterface StatCardProps {\n  label: string;\n  value: string;\n  delta?: string;\n}\n\nexport function StatCard({ label, value, delta }: StatCardProps) {\n  return (\n    <div style={{\n      background: '{{token:colors.base.surface}}',\n      border: '1px solid {{token:colors.base.border}}',\n      borderRadius: '{{token:spacing.card.borderRadius}}',\n      padding: '{{token:spacing.card.padding}}',\n      display: 'flex', flexDirection: 'column', gap: '{{token:spacing.card.gap}}',\n    }}>\n      <span style={{ color: '{{token:colors.text.secondary}}', fontSize: '{{token:typography.scale.sm}}' }}>{label}</span>\n      <span style={{ color: '{{token:colors.text.primary}}', fontSize: '{{token:typography.scale.2xl}}', fontWeight: 700 }}>{value}</span>\n      {delta ? (\n        <span style={{ color: '{{token:colors.accent.success}}', fontSize: '{{token:typography.scale.xs}}' }}>{delta}</span>\n      ) : null}\n    </div>\n  );\n}\n"
}