{
  "name": "Modal",
  "category": "feedback",
  "description": "Overlay modal with elevated surface, title, and close action.",
  "propsSchema": {
    "title": { "type": "string", "required": false, "description": "Modal title" },
    "children": { "type": "ReactNode", "required": false, "description": "Modal body" }
  },
  "template": "import React from 'react';\n\nexport function Modal({ title, children }: { title?: string; children?: React.ReactNode }) {\n  return (\n    <div style={{ position: 'fixed', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', background: '{{token:colors.base.overlay}}' }}>\n      <div style={{\n        background: '{{token:colors.base.surface}}',\n        borderRadius: '{{token:spacing.card.borderRadius}}',\n        padding: '{{token:spacing.card.padding}}',\n        border: '1px solid {{token:colors.base.border}}',\n        boxShadow: '0 8px 32px {{token:colors.glass.shadow}}',\n        minWidth: '360px',\n      }}>\n        {title ? <h3 style={{ color: '{{token:colors.text.primary}}', fontSize: '{{token:typography.scale.lg}}' }}>{title}</h3> : null}\n        {children}\n      </div>\n    </div>\n  );\n}\n"
}